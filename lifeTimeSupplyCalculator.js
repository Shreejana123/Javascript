var currentAge = 10;
var maxAge = 70;
var estimatedDay = 3;
var totalNeeded = (estimatedDay * 365) * (maxAge - currentAge);
var result = 'You will need ' + totalNeeded + ' times of snack to last you until the ripe old age of ' + maxAge;
console.log(result);